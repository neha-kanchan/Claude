{
  "name": "sma-housing-system",
  "version": "3.0.0",
  "description": "SMA Student Housing Management System - React (Vite) frontend + NestJS API over Prisma.",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run env:init --prefix backend && npm run db:init",
    "db:init": "npm run prisma:deploy --prefix backend",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=22"
  }
}
